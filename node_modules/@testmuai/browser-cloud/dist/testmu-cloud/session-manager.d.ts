import { SessionConfig, Session, ReleaseResponse, LiveDetailsResponse } from './types.js';
import { TunnelService } from './services/tunnel-service.js';
import { ExtensionService } from './services/extension-service.js';
export declare class SessionManager {
    private tunnelService;
    private extensionService;
    private eventsService;
    constructor();
    setTunnelService(service: TunnelService): void;
    setExtensionService(service: ExtensionService): void;
    createSession(config: SessionConfig): Promise<Session>;
    listSessions(): Session[];
    getSession(id: string): Session | undefined;
    releaseSession(id: string): Promise<ReleaseResponse>;
    /**
     * Release all active sessions
     */
    releaseAll(): Promise<ReleaseResponse>;
    /**
     * Get recorded events for a session (RRWeb format)
     */
    getSessionEvents(id: string): import("./types.js").SessionEvent[];
    /**
     * Get event count for a session
     */
    getEventCount(id: string): number;
    /**
     * Get live details for a session (pages, tabs, state)
     */
    getLiveDetails(id: string): Promise<LiveDetailsResponse | null>;
}
//# sourceMappingURL=session-manager.d.ts.map
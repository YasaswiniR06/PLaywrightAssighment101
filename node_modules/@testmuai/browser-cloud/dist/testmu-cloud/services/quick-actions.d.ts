import { Page } from 'puppeteer-core';
import { ScrapeParams, ScrapeResponse, ScreenshotParams, ScreenshotResponse, PdfParams, PdfResponse } from '../types.js';
/**
 * QuickActionsService - TestMu AI Browser Cloud-compatible Quick Actions
 *
 * Provides quick operations for scraping, screenshots, and PDFs.
 * Can operate standalone or use existing sessions.
 */
export declare class QuickActionsService {
    private sessionPages;
    /**
     * Register a page for a session (for session-based quick actions)
     */
    registerSessionPage(sessionId: string, page: Page): void;
    /**
     * Unregister a session page
     */
    unregisterSessionPage(sessionId: string): void;
    /**
     * Get a page - either from existing session or create transient browser
     */
    private getPage;
    /**
     * Wait for page to be ready
     */
    private waitForPage;
    /**
     * Scrape a webpage
     */
    scrape(params: ScrapeParams | string): Promise<ScrapeResponse>;
    /**
     * Take a screenshot
     */
    screenshot(params: ScreenshotParams | string, fullPage?: boolean): Promise<ScreenshotResponse | Buffer>;
    /**
     * Generate PDF
     */
    pdf(params: PdfParams | string): Promise<PdfResponse | Buffer>;
}
//# sourceMappingURL=quick-actions.d.ts.map
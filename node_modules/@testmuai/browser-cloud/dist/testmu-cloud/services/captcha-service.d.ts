import { CaptchaSolveParams, CaptchaSolveResponse, CaptchaStatusResponse } from '../types.js';
/**
 * CaptchaService - Captcha Solving Integration
 *
 * Supports integration with external services like 2Captcha, Anti-Captcha,
 * or LambdaTest's built-in solving.
 *
 * Configure via environment variables:
 * - CAPTCHA_API_KEY: API key for the captcha service
 * - CAPTCHA_SERVICE: Service to use ('2captcha', 'anticaptcha', 'lambdatest')
 */
export declare class CaptchaService {
    private jobs;
    private apiKey;
    private service;
    constructor();
    solveImage(sessionId: string, params: CaptchaSolveParams): Promise<CaptchaSolveResponse>;
    status(sessionId: string): Promise<CaptchaStatusResponse>;
    /**
     * Get status by job ID
     */
    getJobStatus(jobId: string): Promise<CaptchaStatusResponse>;
    /**
     * Process captcha (simulated or real API call)
     */
    private processCapture;
    /**
     * Call external captcha solving service
     */
    private callExternalService;
    /**
     * 2Captcha API integration (placeholder)
     */
    private solve2Captcha;
    /**
     * Anti-Captcha API integration (placeholder)
     */
    private solveAntiCaptcha;
    /**
     * TestMu AI Browser Cloud captcha solving (placeholder)
     */
    private solveLambdaTest;
    /**
     * Simulate delay for development/testing
     */
    private simulateDelay;
    /**
     * @deprecated Use solveImage() instead
     */
    solve(params: any): Promise<{
        id: string;
        status: string;
        code?: string;
    }>;
    /**
     * @deprecated Use getJobStatus() instead
     */
    getResult(id: string): Promise<{
        id: string;
        status: string;
        code: string | undefined;
    }>;
}
//# sourceMappingURL=captcha-service.d.ts.map
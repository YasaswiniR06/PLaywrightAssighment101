import { Page } from 'puppeteer-core';
import { ComputerActionParams, ComputerActionResponse } from '../types.js';
/**
 * ComputerService - AI Agent Mouse/Keyboard Control
 *
 * Provides programmatic control for AI agents to interact with browser sessions
 * using mouse movements, clicks, keyboard input, and screenshots.
 *
 * This is a critical feature for AI agent use cases (like Claude computer use).
 */
export declare class ComputerService {
    /**
     * Execute a computer action on a page
     */
    execute(page: Page, params: ComputerActionParams): Promise<ComputerActionResponse>;
    /**
     * Take a screenshot and return as base64
     */
    screenshot(page: Page, fullPage?: boolean): Promise<string>;
    /**
     * Move mouse to coordinates
     */
    move(page: Page, x: number, y: number): Promise<void>;
    /**
     * Click at coordinates
     */
    click(page: Page, x: number, y: number): Promise<void>;
    /**
     * Type text
     */
    type(page: Page, text: string): Promise<void>;
    /**
     * Press a key
     */
    press(page: Page, key: string): Promise<void>;
    /**
     * Scroll the page
     */
    scroll(page: Page, deltaX: number, deltaY: number): Promise<void>;
}
//# sourceMappingURL=computer-service.d.ts.map